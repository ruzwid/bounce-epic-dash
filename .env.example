# Copy this to .env.local and fill in real values. .env.local is gitignored
# (via the *.local glob in .gitignore) — never commit real tokens.
JIRA_BASE_URL=
JIRA_EMAIL=
JIRA_API_TOKEN=
GITHUB_TOKEN=
