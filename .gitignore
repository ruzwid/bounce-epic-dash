node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
.vercel
__unconfig*
todos.json

# Data pipeline: raw/pending/judgment hold full-fidelity JIRA/GitHub data and
# untrusted judge output — never publication-safe, never committed. Only
# data/snapshots/ (produced by scripts/merge.ts) is committed.
data/raw/
data/pending/
data/judgment/
*.tmp
